# Lesson 03: device_global

This lesson shows a namespace-scope `device_global` used from inside a kernel.
That causes the device integration footer to emit device-global registration
code.

Regenerate with:

```bash
bash tutorial/generate_artifacts.sh Lesson-03
```